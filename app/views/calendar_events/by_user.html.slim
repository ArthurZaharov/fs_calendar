h3
	= link_to users_calendar_events_path(for_date: "#{@events_date - 1.day}") do
		span.glyphicon.glyphicon-chevron-left
	| Your events for #{@events_date.strftime("%B, %d")}
	= link_to users_calendar_events_path(for_date: "#{@events_date + 1.day}") do
		span.glyphicon.glyphicon-chevron-right

- unless @calendar_events.empty?
	= render @calendar_events
- else
	em You have not events for this day
